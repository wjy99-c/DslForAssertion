int function77(){if (sum[i]<0){
    bool flag=true;
    MyDeviceToHostSideChannel_Overflow::write(i,flag);
     channel_sum[0] = channel_sum[0] + 1;
}
                                          }
                  );

  });
}